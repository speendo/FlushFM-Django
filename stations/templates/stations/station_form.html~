{% extends "base_with_header.html" %}
{% load staticfiles %}
{% load bootstrap3 %}

{% block pagetitle %}
New Station
{% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}
{% endblock %}

{% block content %}
<form action="" method="post">
	{% csrf_token %}
	
		{{ form.name.errors }}
		{% bootstrap_field form.name %}

		{{ form.description.errors }}
		{% bootstrap_field form.description %}

	<fieldset id="addresses">
		<legend>Web Addresses:</legend>
		{% for a_form in address_formset %}
			{% bootstrap_field a_form.url %}
			<script type='text/javascript'>
				var addressTemplate = '<div class="addresses_set">{% bootstrap_field a_form.url %}</div>';
			</script>
			<script type='text/javascript' src='{% static 'stations/createStationFormsets.js' %}'></script>
		{% endfor %}
		{{ address_formset.management_form }}
	</fieldset>
    <button type="submit" class="btn btn-primary">Create</button>
</form>
{% endblock %}
