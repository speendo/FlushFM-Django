{% extends "base_with_header.html" %}

{% block pagetitle %}
List of Webradio Stations
{% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}
{% endblock %}

{% block content %}
    <table class="table table-striped table-hover">
		<thead>
			<tr>
				<th>Station</th>
				<th>Genres</th>
				<th>Addresses</th>
			</tr>
		</thead>
		{% for station in stations %}
			<tr>
				<th><h2>{{ station.name }}<br><small>{{ station.description }}</small></h2></th>
				<th>
					<ul class="list-group">
	            	{% for genre in station.genres.all %}
	            		<li class="list-group-item"><button type="button" class="btn" style="background-color: #{{ station.genre.color }};">{{ genre.name }}</button></li>
	            	{% endfor %}
	            	</ul>
	            </th>
	            <th>
					<ul class="list-group">
	            	{% for address in station.address_set.all %}
	            		<li class="list-group-item">{{ address.url }}</li>
	            	{% endfor %}
	            	</ul>
	            </th>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
